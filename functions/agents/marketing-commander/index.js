// marketing-commander — owns campaign strategy & broadcasts (reconciles with the
// existing `campaigns` collection). Level 2: broadcasts/campaigns need approval.
// Permissions/level in ../_shared/permissions.js. Phase 1: skeleton, no functions.
module.exports = {};
