
# Firebase service account keys — never commit
staging-service-account.json
prod-service-account.json

# Local migration/dev scripts — contain hardcoded Firebase config
migrate-products.js
migrate-products-browser.js
add-bo2.html
add-bo2.mjs
update-bo2-cat.mjs
functions/node_modules

# Dependencies
node_modules/

# Local Netlify folder
.netlify

# Jarvis lead-intelligence snapshots / backups — contain lead PII, never commit
scripts/_snapshots/
*lead-intelligence-snapshot-*.json
# Generated render harness embeds live lead data — never commit
scripts/.phase4-harness.html
# Baseline benchmark embeds lead names (PII) — keep local, never commit
JARVIS_BASELINE_*.md
